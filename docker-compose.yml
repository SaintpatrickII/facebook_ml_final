version: '3.4'

services:
  api-service:
    # build: 
    #   context: .
    #   dockerfile: dockerfolder/dockerfile
    image: patrickgovus/test
    volumes:
      - ~/dockerfolder:/app
    ports:
      - 8080:8080
    restart: always
    network_mode: host
